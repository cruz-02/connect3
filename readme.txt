To run the different programs:

Play against it:
python main.py

for playing local server (own machine):
python main.py --mode server

for playing on trlinux machines:
python main.py --mode server --server-type prof --host-number X

flags
--grid Large